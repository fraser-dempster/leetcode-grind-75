/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
var addBinary = function (a, b) {
  // use big int and tell it the number if binary
  // then to string to convert back to binary string
  // if have time try and build the binary addition manually using lots of if statements and looping through the binary strings backwardsthub
};
