/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var search = function (nums, target) {
  // check if nums is empty or null
  // set left and right indexes
  // left <= right
  // set mid index
  // if middle is elemnent then return mid index
  // if nums[mid] is less than set left to mid + 1
  // else right = mid - 1
  // if nothing return -1
};
