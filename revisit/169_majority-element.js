/**
 * @param {number[]} nums
 * @return {number}
 */
var majorityElement = function (nums) {
  // can use a map and a single loop
  // create the map using map[n] = (map[n] || 0) + 1
  // then check the your max is less than the map[n]
  // if it is the answer is equal to the current n
  // you can also use boyer moore majority vote algorithm
  // look up how that works and try implement it
};
