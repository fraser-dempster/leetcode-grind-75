/**
 * @param {string} ransomNote
 * @param {string} magazine
 * @return {boolean}
 */
var canConstruct = function (ransomNote, magazine) {
  // create a map for the magazine letters
  // loop through the random note and decrement to check if char is in magazine map
};
