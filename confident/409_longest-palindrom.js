/**
 * @param {string} s
 * @return {number}
 */
var longestPalindrome = function (s) {
  // solution requires you know that and even number of characters means two possible palindromes could be made
  // count characters and create answer from count as you loop through the string
};
